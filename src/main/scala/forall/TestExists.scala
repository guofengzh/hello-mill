package forall

object ExistsMain {

  /*  
  def foo(unapplied: List[Exists[[A] => A => (A, A => String)]]): List[String] =
    unapplied map { u =>
      val (v, f) = u.value
      f(v)
    }  


  def main(args: Array[String]):Unit = {
      val r = foo(
            List(
              Exists[[A] => A => (A, A => String)]((42, (i: Int) => i.toString)),
              Exists[[A] => A => (A, A => String)](("hi", (s: String) => s))
            )
        )
      println(r)
  } 
  */
}

